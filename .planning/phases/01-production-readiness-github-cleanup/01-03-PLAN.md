---
phase: 01-production-readiness-github-cleanup
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - README.md
autonomous: true
requirements:
  - REQ-05
must_haves:
  truths:
    - "README.md exists at the project root"
    - "README.md covers: description, features, prerequisites, setup, configuration table, usage, project structure, development, SYSTEM_PROMPT customization note"
    - "README.md is written in English"
    - "README.md mentions the pre-commit hook installed by Plan B"
    - "README.md project structure reflects clean state after Plan A (no plans/ or __pycache__)"
  artifacts:
    - path: "README.md"
      provides: "Professional English entry-point documentation for GitHub visitors"
      min_lines: 80
      contains: "## Setup"
  key_links:
    - from: "README.md"
      to: ".env.example"
      via: "setup instructions reference cp .env.example .env"
      pattern: "cp .env.example .env"
    - from: "README.md"
      to: "main.py"
      via: "usage section documents python main.py and --model flag"
      pattern: "python main.py"
---

<objective>
Create a professional, English-language README.md at the project root that serves
as the entry point for any GitHub visitor. This plan runs after Plans A and B so
the README accurately reflects the clean final state of the repository (no plans/
tracked, rich pinned, pre-commit hook documented).

Purpose: The repository currently has no README.md — the most basic signal of a
professional project. A well-structured README communicates project purpose,
teaches setup, and demonstrates polish for portfolio or educational use.

Output:
- README.md at the project root with all required sections (per D-05)
- A git commit recording the new file
</objective>

<execution_context>
@/Users/konrad.malinowski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/konrad.malinowski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-production-readiness-github-cleanup/01-01-SUMMARY.md
@.planning/phases/01-production-readiness-github-cleanup/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create professional README.md in English (D-05)</name>
  <files>README.md</files>
  <read_first>
    - main.py — lines 1-30 (imports, constants used, CLI argument definition) and
      lines 150-165 (argparse setup showing --model flag and description string)
    - constants.py — full file (36 lines): DEFAULT_SESSION_ID, MAX_INPUT_LENGTH,
      SESSIONS_DIR, SYSTEM_PROMPT (to describe the SYSTEM_PROMPT customization section)
    - .env.example — full file (post-Plan-B, will have warning header + 2 variables)
    - requirements.txt — full file (to list Python version compatibility and direct deps)
    - requirements-dev.txt — full file (2 lines: pytest, ruff==0.11.13)
    - docs/ — list directory to know Polish-language stage docs exist (do not describe
      them in detail in README, just note they exist if relevant)
  </read_first>
  <action>
    Create README.md at the project root with the following structure and content.
    Write in clear, professional English. Do NOT use emoji. Keep sections concise.

    --- BEGIN README CONTENT ---

    # chatbot-app

    An educational CLI chatbot built with [LangChain](https://python.langchain.com/)
    and Google Gemini (`gemini-2.5-flash`). Demonstrates LangChain LCEL pipelines,
    conversation history management, and terminal output with Rich.

    ## Features

    - **Model selection** — choose any Gemini model at runtime via `--model`
    - **Session persistence** — conversation history saved to `sessions/` as JSON and
      restored on next run
    - **Rich terminal output** — colored prompts via the `rich` library
    - **Input validation** — rejects inputs exceeding 2 000 characters
    - **Startup checks** — clear error messages when `GEMINI_API_KEY` is missing or
      when `SYSTEM_PROMPT` still contains unfilled placeholders

    ## Prerequisites

    - Python 3.11 or higher
    - A [Google AI Studio](https://aistudio.google.com/app/apikey) API key

    ## Setup

    ```bash
    # 1. Clone the repository
    git clone <repository-url>
    cd chatbot-app

    # 2. Create and activate a virtual environment
    python -m venv .venv
    source .venv/bin/activate      # Windows: .venv\Scripts\activate

    # 3. Install dependencies
    pip install -r requirements.txt

    # 4. Configure environment variables
    cp .env.example .env
    # Edit .env and set your GEMINI_API_KEY
    ```

    > **Security note:** A pre-commit hook is included in `.git/hooks/pre-commit`
    > that prevents accidentally committing the `.env` file. It is installed
    > automatically when you clone the repository and activate the hook directory.
    > If you need to install it manually: `chmod +x .git/hooks/pre-commit`.

    ## Configuration

    | Variable | Required | Description |
    |----------|----------|-------------|
    | `GEMINI_API_KEY` | Yes | Google AI Studio API key |
    | `GEMINI_LLM_MODEL` | Yes | Gemini model name (e.g. `gemini-2.5-flash`) |

    Copy `.env.example` to `.env` and fill in both values. Never commit `.env`.

    ## Usage

    ```bash
    # Start the chatbot with the model from .env
    python main.py

    # Override the model at runtime
    python main.py --model gemini-1.5-pro
    ```

    Exit the chat by submitting an empty line (press Enter on a blank prompt).

    ## Project Structure

    ```
    chatbot-app/
    ├── main.py            # Entry point — CLI loop, LangChain chain, session wiring
    ├── constants.py       # SYSTEM_PROMPT, DEFAULT_SESSION_ID, MAX_INPUT_LENGTH
    ├── session_store.py   # JSON session persistence utilities
    ├── requirements.txt   # Pinned runtime dependencies
    ├── requirements-dev.txt  # Dev tools: pytest, ruff
    ├── .env.example       # Environment variable template (safe to commit)
    ├── docs/              # Polish-language stage documentation
    └── sessions/          # Runtime session files (git-ignored)
    ```

    ## Customising the System Prompt

    `constants.py` contains `SYSTEM_PROMPT`, a Polish-language chatbot persona
    template. It includes bracketed placeholders such as `[NAZWA FIRMY / PROJEKTU]`
    and `[GŁÓWNY CEL BOTA]` that must be replaced with real values before deploying
    the bot for any specific use case.

    Open `constants.py` and substitute every `[...]` placeholder with your project's
    name, goal, contact information, and topic areas.

    ## Development

    ```bash
    # Install dev tools
    pip install -r requirements-dev.txt

    # Lint and auto-fix
    ruff check --fix .

    # Format
    ruff format .
    ```

    ## License

    Educational use. No license file is included — treat this as a learning reference,
    not production software.

    --- END README CONTENT ---

    After writing README.md, stage and commit it:
      git add README.md
      git commit -m "docs: add professional English README"
  </action>
  <verify>
    <automated>test -f README.md && wc -l README.md && grep -c "## Setup" README.md && grep -c "DO NOT COMMIT\|pre-commit hook" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f README.md` exits 0 (file exists)
    - `wc -l README.md` outputs a number >= 80 (substantive content)
    - `grep -c "## Setup" README.md` outputs `1`
    - `grep -c "## Features" README.md` outputs `1`
    - `grep -c "## Configuration" README.md` outputs `1`
    - `grep -c "## Usage" README.md` outputs `1`
    - `grep -c "## Project Structure" README.md` outputs `1`
    - `grep -c "## Customising the System Prompt" README.md` outputs `1`
    - `grep -c "## Development" README.md` outputs `1`
    - `grep -c "pre-commit" README.md` outputs >= `1` (hook mentioned)
    - `grep -c "GEMINI_API_KEY" README.md` outputs >= `1` (configuration table present)
    - `grep -c "python main.py" README.md` outputs >= `1` (usage documented)
    - `grep -c "cp .env.example .env" README.md` outputs `1` (setup step present)
    - `git show --name-only HEAD | grep README.md` confirms it is in the commit
    - README.md is written entirely in English (no Polish text except SYSTEM_PROMPT
      placeholder examples which are reproduced verbatim as code)
  </acceptance_criteria>
  <done>
    README.md exists at the project root with all nine required sections, written in
    English, committed to git.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md → public GitHub | README is public-facing; must not embed any secret values or internal paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-C | Information Disclosure | README.md | mitigate | Review that README contains only placeholder values in examples (e.g., `your_google_gemini_api_key_here`), never a real key |
| T-01-02-C | Information Disclosure | README.md project structure section | accept | Lists file names only; no internal paths, no .planning/ or plans/ directory listed since those are gitignored after Plan A |
</threat_model>

<verification>
After the task completes:

```bash
# File exists and is substantial
test -f README.md && echo "exists"
wc -l README.md   # >= 80 lines

# Required sections present
for section in "## Features" "## Prerequisites" "## Setup" "## Configuration" "## Usage" "## Project Structure" "## Customising the System Prompt" "## Development"; do
  grep -q "$section" README.md && echo "OK: $section" || echo "MISSING: $section"
done

# Security-relevant content
grep "pre-commit" README.md   # hook mentioned
grep "GEMINI_API_KEY" README.md  # config table present
grep "cp .env.example .env" README.md  # setup step present

# Committed
git log --oneline -1   # should show "docs: add professional English README"
```
</verification>

<success_criteria>
- README.md exists at project root with >= 80 lines
- All nine sections present: Features, Prerequisites, Setup, Configuration, Usage,
  Project Structure, Customising the System Prompt, Development, License
- Pre-commit hook security note included in Setup section
- Configuration table documents both GEMINI_API_KEY and GEMINI_LLM_MODEL
- Written entirely in English
- Committed to git
</success_criteria>

<output>
Create `.planning/phases/01-production-readiness-github-cleanup/01-03-SUMMARY.md` when done.
</output>
