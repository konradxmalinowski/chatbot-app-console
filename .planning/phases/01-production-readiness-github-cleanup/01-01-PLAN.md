---
phase: 01-production-readiness-github-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
autonomous: true
requirements:
  - REQ-01
  - REQ-02
  - REQ-06
must_haves:
  truths:
    - "git ls-files __pycache__ returns empty output"
    - "git ls-files plans/ returns empty output"
    - ".gitignore contains entries for .idea/, plans/, .ruff_cache/"
    - "sessions/ is already in .gitignore (verify retained)"
  artifacts:
    - path: ".gitignore"
      provides: "Comprehensive exclusion rules for dev/IDE/internal artifacts"
      contains: "plans/"
  key_links:
    - from: ".gitignore"
      to: "git tracking"
      via: "git rm --cached removes already-tracked files"
      pattern: "plans/"
---

<objective>
Remove Python bytecode and internal planning files from git tracking, and expand
.gitignore to cover all development/IDE artifacts that should never appear on a
public GitHub repository.

Purpose: The repository currently tracks __pycache__/constants.cpython-314.pyc
(visible as "modified" in git status) and plans/2026-06-06-code-quality-improvements.md
(untracked but not gitignored). Both must be removed from tracking and permanently
excluded. The .gitignore is also missing entries for .idea/, plans/, and .ruff_cache/.
Note: .planning/ stays tracked per D-06 (it documents project evolution).

Output:
- .gitignore updated with .idea/, plans/, .ruff_cache/ entries
- __pycache__ untracked via git rm --cached
- plans/ untracked via git rm --cached -r
- A git commit recording the tracking removal
</objective>

<execution_context>
@/Users/konrad.malinowski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/konrad.malinowski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand .gitignore with missing exclusions (D-02, D-06)</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore — read the full file before modifying; current content is 11 lines
    covering .env, .idea, .venv/, __pycache__/, *.pyc, *.pyo, .pytest_cache/,
    dist/, build/, *.egg-info/, sessions/
  </read_first>
  <action>
    Open .gitignore and append the following entries after the existing content.
    Do NOT remove any existing lines. Add exactly these new lines (one per entry,
    with a blank separator line and comment before each group):

    After the existing `sessions/` line, add:

    ```
    # IDE config
    .idea/

    # Internal planning notes (raw developer notes; .planning/ stays tracked per D-06)
    plans/

    # Ruff cache
    .ruff_cache/
    ```

    Note: `.idea` is already present on line 2 WITHOUT a trailing slash. Keep
    the existing `.idea` entry AND add `.idea/` so both bare-name and directory
    forms are covered. If the existing line already reads `.idea/` (with slash),
    do not duplicate it — only add the missing entries from the list above.

    Do NOT add `.planning/` to .gitignore — it must remain tracked per D-06.

    After editing, verify the file with:
      grep -E "^(plans/|\.ruff_cache/)" .gitignore
    Expected: two matching lines printed.
  </action>
  <verify>
    <automated>grep -c "^plans/$" .gitignore && grep -c "^\.ruff_cache/$" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^plans/$" .gitignore` outputs `1`
    - `grep -c "^\.ruff_cache/$" .gitignore` outputs `1`
    - `.planning/` does NOT appear in .gitignore (per D-06, it stays tracked)
    - All pre-existing entries still present: `grep -c "^\.env$" .gitignore` outputs `1`,
      `grep -c "^sessions/$" .gitignore` outputs `1`,
      `grep -c "^__pycache__/$" .gitignore` outputs `1`
  </acceptance_criteria>
  <done>
    .gitignore contains all required exclusions; .planning/ is absent from .gitignore
    (stays tracked); no pre-existing entry removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Untrack __pycache__ and plans/ from git (D-01, D-06)</name>
  <files>.gitignore</files>
  <read_first>
    - Run `git ls-files __pycache__` to confirm what is tracked before running rm --cached
    - Run `git ls-files plans/` to confirm what is tracked
    - .gitignore must be updated (Task 1) before this task runs so the untracked
      files stay excluded after removal
  </read_first>
  <action>
    Run the following commands in sequence:

    1. Remove __pycache__ from git index (per D-01):
       git rm --cached -r __pycache__/

    2. Remove plans/ from git index (per D-06):
       git rm --cached -r plans/

    3. Stage the updated .gitignore:
       git add .gitignore

    4. Commit all tracking removals together:
       git commit -m "chore(git): untrack pycache, plans/ and expand .gitignore"

    After the commit, verify with:
       git ls-files __pycache__
       git ls-files plans/
    Both must produce empty output.

    Also confirm git status is clean (no modified tracked files from pycache):
       git status --short | grep "M __pycache__" | wc -l
    Expected: 0
  </action>
  <verify>
    <automated>git ls-files __pycache__ | wc -l && git ls-files plans/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files __pycache__` produces zero lines of output
    - `git ls-files plans/` produces zero lines of output
    - `git status --short` does NOT contain any line referencing __pycache__ or plans/
    - A commit exists with message containing "untrack pycache" (verifiable via
      `git log --oneline -1`)
    - .gitignore changes are included in the same commit (verifiable via
      `git show --name-only HEAD | grep .gitignore`)
  </acceptance_criteria>
  <done>
    Neither __pycache__ nor plans/ appear in git ls-files output; the tracking
    removal is recorded in a commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem → git index | git add/rm --cached operations modify what is tracked |
| .gitignore rules → git tracking | Rules must be present before rm --cached so files stay excluded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-A | Information Disclosure | plans/ directory | mitigate | Remove from git tracking via git rm --cached; add plans/ to .gitignore so internal planning docs are never pushed to public remote |
| T-01-02-A | Information Disclosure | __pycache__/ | mitigate | Remove bytecode from git index; already in .gitignore but was tracked before rule existed |
| T-01-03-A | Information Disclosure | .idea/ | mitigate | Add .idea/ to .gitignore; IDE config may contain developer-specific paths or credentials |
</threat_model>

<verification>
After both tasks complete:

```bash
git ls-files __pycache__   # must print nothing
git ls-files plans/        # must print nothing
grep "^plans/$" .gitignore  # must print: plans/
grep "^\.ruff_cache/$" .gitignore  # must print: .ruff_cache/
git log --oneline -1       # must show the cleanup commit
```
</verification>

<success_criteria>
- __pycache__ entirely removed from git tracking
- plans/ entirely removed from git tracking
- .gitignore has entries for: plans/, .ruff_cache/ (in addition to all pre-existing entries)
- .planning/ is NOT in .gitignore — it remains tracked per D-06
- All changes committed in a single clean commit
- `git status` shows no modified tracked files from pycache
</success_criteria>

<output>
Create `.planning/phases/01-production-readiness-github-cleanup/01-01-SUMMARY.md` when done.
</output>
