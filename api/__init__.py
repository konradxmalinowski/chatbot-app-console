"""FastAPI REST API for chatbot-app (Phase 3).

Run with: uvicorn api.main:app

This package is an additional entry point alongside the CLI (main.py) — it does
not replace it, and the CLI's behavior is unaffected by anything in here.
"""
