annotated-types==0.7.0
rich==15.0.0
anyio==4.13.0
certifi==2026.5.20
cffi==2.0.0
charset-normalizer==3.4.7
cryptography==48.0.1
distro==1.9.0
google-api-core==2.31.0
google-auth==2.53.0
google-genai==2.7.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
jsonpatch==1.33
jsonpointer==3.1.1
langchain==1.3.9
langchain-core==1.4.8
langchain-google-genai==4.2.4
langchain-protocol==0.0.18
orjson==3.11.9
packaging==26.2
pyasn1==0.6.3
pyasn1_modules==0.4.2
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
python-dotenv==1.2.2
PyYAML==6.0.3
requests==2.34.2
requests-toolbelt==1.0.0
sniffio==1.3.1
tenacity==9.1.4
typing-inspection==0.4.2
typing_extensions==4.15.0
urllib3==2.7.0
uuid_utils==0.16.0
websockets==15.0.1

# --- RAG pipeline (Phase 2) ---
# Direct dependencies:
#   chromadb            — vector store engine
#   langchain-chroma    — LangChain VectorStore wrapper around chromadb
#   pypdf               — PDF text extraction for rag/loader.py
#   langchain-ollama    — OllamaEmbeddings (default, no cloud key required)
#   langchain-openai    — OpenAIEmbeddings (used when EMBEDDINGS_PROVIDER=openai)
#   langchain-text-splitters — RecursiveCharacterTextSplitter for rag/chunker.py
# Everything else below is a transitive dependency of the above (chromadb in
# particular pulls in a FastAPI/Uvicorn/OpenTelemetry/ONNX Runtime stack since it
# can also run as a standalone server).
aiohappyeyeballs==2.7.1
aiohttp==3.14.1
aiosignal==1.4.0
annotated-doc==0.0.4
attrs==26.1.0
bcrypt==5.0.0
build==1.5.0
chromadb==1.5.9
click==8.4.2
durationpy==0.10
filelock==3.29.5
filetype==1.2.0
flatbuffers==25.12.19
frozenlist==1.8.0
fsspec==2026.6.0
googleapis-common-protos==1.75.0
grpcio==1.82.0
hf-xet==1.5.1
httptools==0.8.0
huggingface_hub==1.22.0
importlib_resources==7.1.0
jiter==0.16.0
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
kubernetes==36.0.2
langchain-chroma==1.1.0
langchain-ollama==1.1.0
langchain-openai==1.3.3
langchain-text-splitters==1.1.2
langgraph==1.2.4
langgraph-checkpoint==4.1.1
langgraph-prebuilt==1.1.0
langgraph-sdk==0.4.2
langsmith==0.9.7
markdown-it-py==4.2.0
mdurl==0.1.2
mmh3==5.2.1
multidict==6.7.1
numpy==2.5.1
oauthlib==3.3.1
ollama==0.6.2
onnxruntime==1.27.0
openai==2.44.0
opentelemetry-api==1.43.0
opentelemetry-exporter-otlp-proto-common==1.43.0
opentelemetry-exporter-otlp-proto-grpc==1.43.0
opentelemetry-proto==1.43.0
opentelemetry-sdk==1.43.0
opentelemetry-semantic-conventions==0.64b0
ormsgpack==1.12.2
overrides==7.7.0
propcache==0.5.2
proto-plus==1.28.0
protobuf==7.35.1
pybase64==1.4.3
pydantic-settings==2.14.2
Pygments==2.20.0
pypdf==6.14.2
PyPika==0.51.1
pyproject_hooks==1.2.0
python-dateutil==2.9.0.post0
referencing==0.37.0
regex==2026.6.28
requests-oauthlib==2.0.0
rpds-py==2026.6.3
shellingham==1.5.4
six==1.17.0
tiktoken==0.13.0
tokenizers==0.23.1
tqdm==4.68.3
typer==0.26.8
uvicorn==0.50.2
uvloop==0.22.1
watchfiles==1.2.0
websocket-client==1.9.0
xxhash==3.8.1
yarl==1.24.2
zstandard==0.25.0

# --- REST API (Phase 3) ---
# Direct dependencies:
#   fastapi   — REST API framework (api/main.py), Swagger UI at /docs is automatic
#   uvicorn   — ASGI server to run the API (`uvicorn api.main:app`); was already
#               present above as a transitive dep of chromadb, now used directly
#   pyjwt     — JWT encode/decode for the /auth/token flow (PyPI dist name: PyJWT)
#   slowapi   — per-token/per-IP rate limiting on /auth/token, /chat, /chat/rag
# starlette, limits, Deprecated, and wrapt below are transitive deps of the above.
Deprecated==1.3.1
fastapi==0.139.0
limits==5.8.0
PyJWT==2.13.0
slowapi==0.1.10
starlette==1.3.1
wrapt==2.2.2

# --- LangGraph Agent with Tools (Phase 4) ---
# Direct dependencies:
#   duckduckgo-search — used by agent/tools.py's web_search tool, no API key needed
#   langgraph, langgraph-checkpoint, langgraph-prebuilt — StateGraph, MemorySaver
#     checkpointer, and the prebuilt ToolNode used by agent/graph.py. These were
#     already present above as transitive deps of langchain-ollama/langchain-openai
#     (Phases 2-3); promoted to documented direct dependencies here since
#     agent/graph.py now imports them directly. Versions unchanged.
duckduckgo-search==8.1.1
lxml==6.1.1
primp==1.3.1

# --- Multi-provider LLM selection (llm_provider.py) ---
# Direct dependency:
#   langchain-anthropic — ChatAnthropic, used when LLM_PROVIDER=anthropic. Pulls in
#     the `anthropic` SDK as a transitive dependency (used directly in main.py/
#     api/main.py for anthropic.AnthropicError exception handling).
#   ChatOpenAI (langchain-openai) and ChatOllama (langchain-ollama), used when
#   LLM_PROVIDER=openai/ollama, are already documented above (Phase 2) — no new
#   package needed for those two providers.
langchain-anthropic==1.4.8
anthropic==0.116.0
docstring-parser==0.18.0
